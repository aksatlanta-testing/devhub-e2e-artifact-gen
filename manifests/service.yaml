apiVersion: v1
kind: Service
metadata:
  name: go_echo
  namespace: devhub-e2e-test-id-1
spec:
  type: LoadBalancer
  selector:
    app: go_echo
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080