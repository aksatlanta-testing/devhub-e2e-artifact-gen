apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo4e79cab3-4189-440a-879d-322fc319e14c.fullname" . }}
  labels:
    {{- include "go-echo4e79cab3-4189-440a-879d-322fc319e14c.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo4e79cab3-4189-440a-879d-322fc319e14c.selectorLabels" . | nindent 4 }}
