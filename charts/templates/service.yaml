apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo17f66aad-252e-4d7c-8396-fee638cf6178.fullname" . }}
  labels:
    {{- include "go-echo17f66aad-252e-4d7c-8396-fee638cf6178.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo17f66aad-252e-4d7c-8396-fee638cf6178.selectorLabels" . | nindent 4 }}
