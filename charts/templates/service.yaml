apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo20015dce-f7d2-438a-be0e-3c66160b1ab5.fullname" . }}
  labels:
    {{- include "go-echo20015dce-f7d2-438a-be0e-3c66160b1ab5.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo20015dce-f7d2-438a-be0e-3c66160b1ab5.selectorLabels" . | nindent 4 }}
