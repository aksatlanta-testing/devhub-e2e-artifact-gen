name: overrides-e2e-helm-workflow-125035b7-39a4-42ef-a148-1ec53bc1d211
"on":
    push:
        branches:
            - devhub-e2e-test-id-2
    workflow_dispatch: {}
env:
    ACR_RESOURCE_GROUP: aksatlanta-testing-devhub-e2e-artifact-gen-dhe2e
    AZURE_CONTAINER_REGISTRY: devhube2e0dacd5171f2046e8aa8da4b9220b2f64
    CHART_OVERRIDE_PATH: charts/values.yaml
    CHART_OVERRIDES: image.repository=devhube2e0dacd5171f2046e8aa8da4b9220b2f64.azurecr.io/helmimage,image.tag=${{ github.sha }},replicaCount=3
    CHART_PATH: charts
    CLUSTER_NAME: aksatlanta-testingdevhub-e2e-artifact-gendhe2e
    CLUSTER_RESOURCE_GROUP: aksatlanta-testing-devhub-e2e-artifact-gen-dhe2e
    CONTAINER_NAME: helmimage
jobs:
    buildImage:
        permissions:
            contents: read
            id-token: write
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: azure/login@92a5484dfaf04ca78a94597f4f19fea633851fa2
              name: Azure login
              with:
                client-id: ${{ secrets.AZURE_CLIENT_ID }}
                subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
                tenant-id: ${{ secrets.AZURE_TENANT_ID }}
            - name: Build and push image to ACR
              run: az acr build --image ${{ env.CONTAINER_NAME }}:${{ github.sha }} --registry ${{ env.AZURE_CONTAINER_REGISTRY }} -g ${{ env.ACR_RESOURCE_GROUP }} -f Dockerfile ./
    deploy:
        permissions:
            actions: read
            contents: read
            id-token: write
        runs-on: ubuntu-latest
        needs:
            - buildImage
        steps:
            - uses: actions/checkout@v3
            - uses: azure/login@92a5484dfaf04ca78a94597f4f19fea633851fa2
              name: Azure login
              with:
                client-id: ${{ secrets.AZURE_CLIENT_ID }}
                subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
                tenant-id: ${{ secrets.AZURE_TENANT_ID }}
            - uses: azure/use-kubelogin@v1
              name: Set up kubelogin for non-interactive login
              with:
                kubelogin-version: v0.0.25
            - uses: azure/aks-set-context@v3
              name: Get K8s context
              with:
                admin: "false"
                cluster-name: ${{ env.CLUSTER_NAME }}
                resource-group: ${{ env.CLUSTER_RESOURCE_GROUP }}
                use-kubelogin: "true"
            - name: Deploy application
              run: helm upgrade --wait -i -f ${{ env.CHART_OVERRIDE_PATH }} --set ${{ env.CHART_OVERRIDES }} go-echo5ea9257f-0f4e-4258-bf53-91f8f0b39f69 ${{ env.CHART_PATH }}
