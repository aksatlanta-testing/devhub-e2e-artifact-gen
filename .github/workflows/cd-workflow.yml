name: Terraform apply infra change - e2e-IacProfile-e93d361b-0f53-4a63-9bb1-6d2b7f9c3f45

on:
  push:
    branches: ["main"]

jobs:
  dev-placeholder:
    name: dev
    needs: []
    runs-on: ubuntu-latest
    steps:
      - run: echo "running dev stage"
  prod-placeholder:
    name: prod
    needs: [qa-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running prod stage"
  qa-placeholder:
    name: qa
    needs: [dev-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running qa stage"
