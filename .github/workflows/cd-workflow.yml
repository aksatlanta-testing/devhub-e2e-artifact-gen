name: Terraform apply infra change - e2e-IacProfile-e6b36fb8-160c-442c-b721-e3cb552b4ff9

on:
  push:
    branches: ["main"]

jobs:
  dev-placeholder:
    name: dev
    needs: []
    runs-on: ubuntu-latest
    steps:
      - run: echo "running dev stage"
  prod-placeholder:
    name: prod
    needs: [qa-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running prod stage"
  qa-placeholder:
    name: qa
    needs: [dev-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running qa stage"
