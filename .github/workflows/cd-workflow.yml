name: Terraform apply infra change - e2e-IacProfile-0c831505-317c-4eba-8e74-a2dcb6239554

on:
  push:
    branches: ["main"]

jobs:
  dev-placeholder:
    name: dev
    needs: []
    runs-on: ubuntu-latest
    steps:
      - run: echo "running dev stage"
  prod-placeholder:
    name: prod
    needs: [qa-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running prod stage"
  qa-placeholder:
    name: qa
    needs: [dev-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running qa stage"
