name: Terraform apply infra change - e2e-IacProfile-c58c913d-e822-4423-9238-25e9088652e5

on:
  push:
    branches: ["main"]

jobs:
  qa-placeholder:
    name: qa
    needs: [dev-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running qa stage"
  dev-placeholder:
    name: dev
    needs: []
    runs-on: ubuntu-latest
    steps:
      - run: echo "running dev stage"
  prod-placeholder:
    name: prod
    needs: [qa-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running prod stage"
