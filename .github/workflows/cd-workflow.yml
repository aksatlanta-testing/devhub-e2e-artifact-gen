name: Terraform apply infra change - e2e-IacProfile-c5c40d0a-84fb-4ddb-82c5-3da4ce934cd1

on:
  push:
    branches: ["main"]

jobs:
  qa-placeholder:
    name: qa
    needs: [dev-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running qa stage"
  dev-placeholder:
    name: dev
    needs: []
    runs-on: ubuntu-latest
    steps:
      - run: echo "running dev stage"
  prod-placeholder:
    name: prod
    needs: [qa-placeholder]
    runs-on: ubuntu-latest
    steps:
      - run: echo "running prod stage"
